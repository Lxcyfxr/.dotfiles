# .dotfiles

In the ~/
```shell-session
git clone https://github.com/Lxcyfxr/.dotfiles.git
mv .zshrc .zshrc.bak
ln -s .dotfiles/.zshrc
```

add .gitconfig:
```shell-session
mv .gitconfig .gitconfig.bak
ln -s .dotfiles/.gitconfig
```
